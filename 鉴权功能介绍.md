# 🔐 鉴权功能说明

> 面向零基础小白，用大白话讲清楚"登录认证"是怎么回事。

---

## 一句话概括

**以前**：谁都能打开你的网站，随便用。

**现在**：必须先输入密码才能用，防止陌生人乱搞。

---

## 基本原理（用"门禁"来理解）

整个过程就像一扇装了 **电子门禁** 的门：

```
你的网站 = 一栋楼
鉴权系统 = 门禁
密码    = 门禁卡/指纹
Token   = 临时通行证（7天内有效）
```

### 用户登录 → 拿到通行证

```
你输入密码 → 后端检查对不对
                 ↓
        ┌ 对 → 发一张"7天有效通行证"(token)
        └ 错 → "密码错误"
```

这个"通行证"是一串乱码，像这样：

```
eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiJ1c2VyIiwiZXhwIjoxNzg0MDEwODM3fQ.H6T5_qXg50y8X0-FDjkpM71fo2O9d7V5yrwun0k2pOs
```

里面**没有你的密码**，只包含"这个人已登录"的证明，和过期时间。

### 拿着通行证访问页面

```
你访问案件列表 → 浏览器自动出示通行证
                    ↓
      后端检查通行证是否有效
                    ↓
        ┌ 有效 → 正常返回数据
        └ 过期/伪造 → "请先登录"
```

---

## 代码到底做了什么？（逐层拆解）

### 后端：3 个核心部分

#### ① 配置文件 — `.env` 里的开关

```
AUTH_ENABLED=true          ← 总开关：开
AUTH_PASSWORD=detective123 ← 登录密码
AUTH_SECRET_KEY=...        ← 用来给通行证盖章的"印章"
```

- 如果你把 `AUTH_ENABLED` 改成 `false`，整个鉴权就关掉了，回到以前随便进的状态
- `AUTH_PASSWORD` 就是登录页面输入的密码
- `AUTH_SECRET_KEY` 是用来加密通行证的，别人不知道这个印章就伪造不了通行证

#### ② 登录接口 — `/api/auth/login`

文件：`backend/app/api/auth.py`

```python
@router.post("/login")
async def login(req: LoginRequest):
    # 1. 检查密码对不对
    if req.password != settings.auth_password:
        raise HTTPException(status_code=401, detail="密码错误")

    # 2. 对的话，发一张通行证
    token = create_access_token({"sub": "user"})
    return {"token": token, "message": "登录成功"}
```

逻辑就是：**收到密码 → 比对 → 对了发通行证，错了拒绝**。

#### ③ 门禁守卫 — 检查通行证的中间人

文件：`backend/app/core/auth.py`

```python
async def get_current_user(credentials):
    # 如果鉴权总开关是关的，直接放行
    if not settings.auth_enabled:
        return None

    # 如果没带通行证 → 401
    if credentials is None:
        raise HTTPException(status_code=401, detail="请先登录")

    # 检查通行证是不是真的、有没有过期
    try:
        payload = jwt.decode(token, settings.auth_secret_key, ...)
        return payload  # 通行证有效，放行
    except JWTError:
        raise HTTPException(status_code=401, detail="登录已过期")
```

### 这个"门禁"装在哪了？

文件：`backend/app/main.py` 的这几行是关键：

```python
# 公开路由（不用通行证）
app.include_router(auth_router)

# 受保护路由（需要通行证）
auth_deps = [Depends(get_current_user)]

app.include_router(cases_router, dependencies=auth_deps)
app.include_router(notes_router, dependencies=auth_deps)
# ... 所有其他路由都一样
```

意思就是：**`/auth/` 开头的路由大家都能访问**（不然没法登录），**其他所有 API 都要检查通行证**。

---

### 前端：3 个核心部分

#### ① 登录页面 — 输入密码的地方

文件：`frontend/src/views/LoginPage.vue`

就是个普通的表单页面：

```
┌─────────────────────┐
│      🔍              │
│   推理助手            │
│   Detective Assistant│
│                      │
│  ┌───────────────┐   │
│  │ 请输入登录密码   │   │
│  └───────────────┘   │
│  ┌───────────────┐   │
│  │     登  录     │   │
│  └───────────────┘   │
└─────────────────────┘
```

用户点击登录 → 前端把密码发给后端 → 后端验证 → 返回通行证 → 前端存到浏览器本地

```javascript
// 登录成功后的关键代码
localStorage.setItem('auth_token', data.token)
router.push('/')
```

`localStorage` 是浏览器自带的一个小仓库，关掉浏览器再打开数据还在。

#### ② 路由守卫 — 自动检查登录状态

文件：`frontend/src/router/index.ts`

每次切换页面时，先检查登录状态：

```javascript
router.beforeEach(async (to, _from, next) => {
  // 如果要去登录页，直接放行
  if (to.name === 'Login') return next()

  // 检查后端的鉴权开关开了没
  let authEnabled = localStorage.getItem('auth_enabled')
  if (authEnabled === null) {
    // 没查过，去问后端
    const res = await fetch('/api/auth/status')
    authEnabled = String(data.enabled)
    localStorage.setItem('auth_enabled', authEnabled)
  }

  // 如果开关开了但是没有通行证 → 踢到登录页
  if (authEnabled === 'true' && !localStorage.getItem('auth_token')) {
    return next({ name: 'Login' })
  }

  // 其他情况放行
  next()
})
```

简单说：**每次进页面都先问 "开鉴权了吗？有通行证吗？" 不行就去登录**。

#### ③ 拦截器 — 每次请求自动带通行证

文件：`frontend/src/api/index.ts`

前端跟后端说话时，自动在请求里带上通行证：

```javascript
// 发请求前，自动在请求头里贴上通行证
api.interceptors.request.use((config) => {
  const token = localStorage.getItem('auth_token')
  if (token) {
    config.headers.Authorization = `Bearer ${token}`
  }
  return config
})

// 收到回复时，如果后端说 401（通行证无效），自动跳到登录页
api.interceptors.response.use(
  (response) => response,
  (error) => {
    if (error.response?.status === 401) {
      localStorage.removeItem('auth_token')
      localStorage.removeItem('auth_enabled')
      window.location.href = '/login'
    }
    return Promise.reject(error)
  }
)
```

有了这个，**前端所有 API 请求都会自动带上通行证，不需要每个页面自己手动处理**。

---

## 完整数据流（从输入密码到看到页面）

```
你输入 "detective123" → 点登录
    │
    ▼
前端 POST /api/auth/login  {password: "detective123"}
    │
    ▼
后端比较密码 → 正确 → 生成 JWT 通行证 → 返回
    │
    ▼
前端收到通行证 → 存到 localStorage
    │
    ▼
路由守卫检测到有通行证了 → 允许访问首页
    │
    ▼
首页发请求 GET /api/cases/
    │  (拦截器自动贴上 Bearer 通行证)
    ▼
后端验证通行证 ✅ → 返回案件数据 → 页面显示
```

---

## 你怎么管理

### 改密码

```bash
ssh root@106.15.238.51
# 改密码
sed -i 's/AUTH_PASSWORD=.*/AUTH_PASSWORD=新密码/' /opt/detective-assistant/backend/.env
# 重启
supervisorctl restart detective
```

### 关闭鉴权（不安全，但本地开发方便）

把服务器 `.env` 里的 `AUTH_ENABLED=true` 删掉，或者改成 `AUTH_ENABLED=false`，然后重启。

### 查看谁在用

这个极简版**没有用户管理**，不管谁登录都是同一个密码。如果想要不同账号，就需要加数据库用户表了，那复杂度会高不少。

---

## 关键词速查表

| 术语 | 大白话 |
|------|--------|
| **鉴权/Auth** | 验证身份的机制 |
| **Token/通行证** | 登录后拿到的一串乱码，证明"我登录过了" |
| **JWT** | 通行证的格式标准，后端用的就是这个 |
| **localStorage** | 浏览器自带的小仓库，关掉浏览器数据还在 |
| **路由守卫** | 一个"门卫"，每次切换页面都检查你有没有通行证 |
| **拦截器** | 一个"自动贴票员"，每次发请求都自动贴上通行证 |
| **401** | HTTP 状态码，意思是"你没权限/没登录" |
